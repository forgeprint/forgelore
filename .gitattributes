# Normalise every text file to LF, in the repository and in the working copy.
# This project is developed on Windows with core.autocrlf enabled; a shell
# script checked out with CRLF fails on its shebang line.
* text=auto eol=lf
